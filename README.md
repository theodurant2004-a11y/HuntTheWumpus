# HuntTheWumpus
pour lancer le site web de manière local il faut se rendre dans le repertoire/la branche à partir du quel on veut lancer l'application
il faut rentrer dans l'environnement virtuel python `source venv/Scripts/activate` une fois dans l'environnement virtuel si flask est bien installé en faisant la commande `flask run`